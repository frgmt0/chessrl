use crate::game::board::Board;
use crate::game::movement::Move;

pub fn is_valid_move(_board: &Board, _mv: &Move) -> bool {
    // lowkey forgot this was here. look in board.rs for piece validation
    true
}
